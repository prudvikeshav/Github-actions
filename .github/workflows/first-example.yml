name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        -  name: Check the repo
           uses: actions/checkout@v4
        -  name: Welcome
           run: 
                |
                echo "My first github Actions"
                ls -lrta
                cat README.md
          
        -  name: List files
           run: ls
        -  name: Read a file
           run: cat README.md
        