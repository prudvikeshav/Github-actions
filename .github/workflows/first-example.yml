name: My first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        -  name: Welcome
           run: echo "My first github Actions"
        -  name: List files
           run: ls